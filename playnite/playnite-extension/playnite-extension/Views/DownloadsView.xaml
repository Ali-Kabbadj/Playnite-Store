<UserControl x:Class="GamesNexus.Views.DownloadsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:app="clr-namespace:GamesNexus.App"
             xmlns:views="clr-namespace:GamesNexus.Views">
    <UserControl.Resources>
        <views:StringToUpperConverter x:Key="StringToUpperConverter" />
        <views:PercentageToWidthConverter x:Key="PercentageToWidthConverter" />

        <!-- Modern Rounded Button Style -->
        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Background" Value="#1AFFFFFF" />
            <Setter Property="Foreground" Value="#CCFFFFFF" />
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="4,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg"
                                Background="{TemplateBinding Background}"
                                CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#33FFFFFF" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Queue &amp; Active Downloads"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White" />
            <TextBlock Text="Manage your game installations and updates."
                       FontSize="14"
                       Foreground="#99FFFFFF"
                       Margin="0,4,0,0" />
        </StackPanel>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Source={x:Static app:GamesNexusContext.DownloadManager}, Path=ActiveDownloads}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="12"
                                Background="#14FFFFFF"
                                BorderBrush="#22FFFFFF"
                                BorderThickness="1"
                                Margin="0,0,0,12"
                                Padding="20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0"
                                            VerticalAlignment="Center"
                                            Margin="0,0,24,0">
                                    <TextBlock Text="{Binding Title}"
                                               Foreground="White"
                                               FontSize="18"
                                               FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis"
                                               Margin="0,0,0,8" />

                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Foreground="#0379FF"
                                                   FontSize="12"
                                                   FontWeight="Bold"
                                                   Text="{Binding StatusName, Converter={StaticResource StringToUpperConverter}}" />

                                        <TextBlock Grid.Column="1"
                                                   Foreground="White"
                                                   FontSize="12"
                                                   FontWeight="Bold"
                                                   Text="{Binding ProgressDisplay}" />
                                    </Grid>

                                    <Border Height="6"
                                            Background="#1AFFFFFF"
                                            CornerRadius="3"
                                            ClipToBounds="True">
                                        <Grid>
                                            <Border Background="#0379FF"
                                                    HorizontalAlignment="Left"
                                                    CornerRadius="3">
                                                <Border.Width>
                                                    <MultiBinding Converter="{StaticResource PercentageToWidthConverter}">
                                                        <Binding Path="Progress" />
                                                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}" />
                                                    </MultiBinding>
                                                </Border.Width>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                                            <TextBlock Text="Speed: " Foreground="#99FFFFFF" FontSize="12" />
                                            <TextBlock Text="{Binding SpeedDisplay}" Foreground="White" FontSize="12" FontWeight="SemiBold" />
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                                            <TextBlock Text="Peers: " Foreground="#99FFFFFF" FontSize="12" />
                                            <TextBlock Text="{Binding PeersDisplay}" Foreground="White" FontSize="12" FontWeight="SemiBold" />
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="ETA: " Foreground="#99FFFFFF" FontSize="12" />
                                            <TextBlock Text="{Binding EtaDisplay}" Foreground="White" FontSize="12" FontWeight="SemiBold" />
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Button Tag="{Binding}"
                                            Click="DownloadPause_Click"
                                            Style="{StaticResource IconBtn}"
                                            ToolTip="Pause/Resume">
                                        <TextBlock Text="⏸" FontSize="12" />
                                    </Button>

                                    <Button Tag="{Binding}"
                                            Click="DownloadRemove_Click"
                                            Style="{StaticResource IconBtn}"
                                            ToolTip="Cancel Download">
                                        <TextBlock Text="✕" FontSize="14" Foreground="#FF453A" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
